"""Provider-neutral adapter execution primitives.

Import concrete construction from ``adapter_runtime.factory`` and lifecycle
composition from ``adapter_runtime.lifecycle``.  This package initializer stays
minimal so provider modules can safely import command and integrity helpers.
"""
